import 'package:flutter/material.dart';
import 'package:project1/View/Widgiets/ReuesWidget.dart';

class Quzie extends StatelessWidget {
  const Quzie({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
